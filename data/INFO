2.4
- баг: preprLocalVerilogDirs поправлен формат для адлека.
- баг: опечатка enumerate в модуле aldec.
- баг: исключение при заданном ucf и не указанным топовым модулем. 

2.4.340
- изменен механизм пойска тулов (быстрее, за счет просмотра путей по умолчанию), первый запуск требует подтверждения (если несколько версий предлагает выбор из списка); формат хранения переделан на yaml;
- решена проблема (№3) для новых версий synplify & xilinx;
- препроцессор поддерживающий след. директивы `include, `define, `ifdef, `elif, `else;
- кэширование инфы о пропарсенных файлах (кэш можно коммитить);

2.4.335:
- отказ от виндового инсталлятора, меньше кликов при установке, да и сам архив меньше в 2 раза;
- deploy и distribute развязаны, версия инкрементируется только по сборке архива;

2.4.329:
- из-за мультипроцессности пришлось переделать механизм логирования (подымается лекговесный сервер) и отрефачить модули. Подробнее здесь
- первая загрузка на сервер (webdav) требует ввода логин\пароль (которые кодируются в base64 сохраняются в <path to python>\Lib\site-packages\autohdl_cfg\open_sesame);

2.4.307
- refactored deployment for proper shutdown log_server (logging stage 2)

2.4.214
- correct logging with multiprocesses, stage 1 (logging server up) 

2.4.140
- мультипроцессорность при парсинге файлов (core i5, cProfile, на небольшом проекте время уменьшилось на 10%);
- запускать питоновкие скрипты можно из под Active-hdl;
- хранение и конвертация параметров происходит через БД (задел на маcштабируемость, быстрый переход на другой тулчейн);

2.4.117
- lives .bit & .mcs files in folder implement
- fixed bug: failed statement 'from autohdl.all import *' with WindowsError exception